import NavBar from './NavBar';

const meta = {
  component: NavBar,
};

export default meta;

export const Default = {
  args: {
    title: "Coffee Shop",
    items: ["Home", "About", "Cart", "Login", "Sign Up"]
  }
};