import NavBar from './NavBar';

const meta = {
  component: NavBar,
};

export default meta;

export const Default = {
  args: {
    title: "Coffee Shop",
    items: ["Home", "About", "Cart", "Login", "Sign Up"]
  }
};

export const Title = {
  args: {
    title: "Brew Haven",
    items: ["Home", "About", "Cart", "Login", "Sign Up"]
  }
};