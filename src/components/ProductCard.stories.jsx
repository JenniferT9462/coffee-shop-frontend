import ProductCard from './ProductCard';

const meta = {
  component: ProductCard,
};

export default meta;

export const Default = {
  args: {
    title: "Latte",
    price: 12.99,
    image: ""
  }
};