import CartItem from './CartItem';

const meta = {
  component: CartItem,
};

export default meta;

export const Default = {
  args: {
    title: "Mocha",
    price: 4.99,
    quantity: 2
  }
};