import SignUpForm from './SignUpForm';

const meta = {
  component: SignUpForm,
};

export default meta;

export const Default = {
  args:{
    buttonLabel:"Sign Up",
    title:"Sign Up Now!"
  }
};

export const SignIn = {
  args: {
    buttonLabel: "Sign In",
    title: "Sign In Now!"
  }
};