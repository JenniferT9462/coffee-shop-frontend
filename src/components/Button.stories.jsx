import Button from './Button';

const meta = {
  component: Button,
};

export default meta;

export const Default = {
  args: {
    label: "Click Me"
  }
};

export const Secondary = {
  args: {
    label: "Get Started"
  }
};

export const LogIn = {
  args: {
    label: "Log In"
  }
};

export const SignUp = {
  args: {
    label: "Sign Up"
  }
};