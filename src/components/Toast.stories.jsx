import Toast from './Toast';

const meta = {
  component: Toast,
};

export default meta;

export const Default = {
  args: {
    type: "success",
    message: "Item added to your cart"
  }
};